# Open Questions for Next Phase

## Runtime Architect

1. Should eventbus be synchronous, async, or dual-mode?
2. What is the minimal backpressure strategy for high-volume VM events?
3. Should snapshots be generated by storage, event aggregator, or IDE shell?

## Storage/Geometry Specialist

1. Should zero writes delete sparse channel entries?
2. Should bounds be static from spec.py or runtime-configurable per crystal profile?
3. What canonical serialization format should be used for coordinates in JSON keys?

## Renderer Specialist

1. What is the renderer diff granularity: voxel, channel, frame batch, or region?
2. What maximum voxel count can be rendered interactively in MVP?
3. Should laser path be rendered from telemetry events or trace records?

## VM Specialist

1. Which opcodes require before/after instrumentation?
2. How much register state should be emitted per step?
3. How are VM errors represented as events?

## Hardware Abstraction Specialist

1. What exact interface separates simulated telemetry from physical drivers?
2. What safety interlocks must exist before a physical driver is enabled?
3. How should calibration files be versioned?

## Physics/Materials Reviewer

1. Which claims require removal, qualification or citation before public release?
2. What material assumptions are safe for simulation-only documentation?
3. What terminology should distinguish theoretical model from tested hardware?
